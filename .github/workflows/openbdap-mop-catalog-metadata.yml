name: openbdap-mop-catalog-metadata

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe-catalog:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
          cache: pip
      - name: Install runtime
        run: python -m pip install -c requirements-runtime.lock -e .
      - name: Probe OpenBDAP MOP catalogue metadata only
        run: |
          mkdir -p artifacts
          python scripts/probe_openbdap_mop_catalog.py > artifacts/openbdap-mop-catalog-metadata.json
      - name: Upload metadata-only result
        uses: actions/upload-artifact@v4
        with:
          name: openbdap-mop-catalog-metadata
          path: artifacts/openbdap-mop-catalog-metadata.json
          if-no-files-found: error
          retention-days: 30
