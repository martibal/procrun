export const dynamic = "force-dynamic";

export default async function SavedPage() {
  return <>
    <h1 className="h1">Keep evidence-bearing opportunities in one review queue.</h1>
    <p className="lede">Saved state is a workspace convenience only. It never changes ProcRun evidence classification or supplier relevance.</p>
    <div className="notice"><strong>Saved persistence is not connected in this local customer preview.</strong> The main project workspace uses the real published customer-safe snapshot; saved selections will be wired when authentication/account persistence is connected.</div>
  </>;
}
